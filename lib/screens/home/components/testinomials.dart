import 'package:flutter/cupertino.dart';

class testinomals extends StatelessWidget {
  const testinomals({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
